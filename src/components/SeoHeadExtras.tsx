/**
 * SEO & discoverability: placeholder for any future head extras.
 * Bing/Pinterest meta are added via metadata.verification.other in lib/metadata.ts
 * so they render in <head> (SSR). This file is kept for documentation.
 */
export function SeoHeadExtras() {
  return null;
}
